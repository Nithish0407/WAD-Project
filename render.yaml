services:
  - type: web
    name: wad-backend
    env: node
    # Use repo root so Frontend/ is included for static serving
    rootDir: .
    buildCommand: cd Backend && npm install
    startCommand: cd Backend && npm start
    autoDeploy: true
