/**
 * Deprecated: single-database mode now stores everything in DB_NAME.
 * Keeping this file as a no-op so accidental runs won't try to reach legacy DBs.
 */
console.log("sync-main-to-computer-db: no-op (single DB mode).");
